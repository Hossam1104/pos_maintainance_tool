# Large Plans

Use this directory only for complex multi-stage work whose plan would make `TASK.md` too large.
`TASK.md` must link to the one active plan. Do not automatically read every plan.
Delete or archive the plan after completion.

There is currently no separate active plan in this directory. The migration sequence and remaining
Session 06–14 prompts stay canonical in `docs/NET10_ANGULAR22_MIGRATION_PLAN.md` and
`docs/NET10_ANGULAR22_SESSION_PROMPTS.md`; the current blocked gate is defined in `TASK.md`.

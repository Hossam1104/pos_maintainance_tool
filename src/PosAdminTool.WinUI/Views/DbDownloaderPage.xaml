<Page
    x:Class="PosAdminTool.WinUI.Views.DbDownloaderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="28" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource HeroBorderStyle}">
            <StackPanel Spacing="6">
                <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="PRODUCTION BACKUP RETRIEVAL" />
                <TextBlock Style="{StaticResource PageTitleTextStyle}" Text="DB Downloader" />
                <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Trigger a branch production DB backup job and download the resulting archives once each branch's zip is validated." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.1*" />
                <ColumnDefinition Width="0.9*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource PanelBorderStyle}">
                <ScrollViewer>
                    <StackPanel Spacing="14">
                        <StackPanel Spacing="4">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Job settings" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="API endpoint and RDB server credentials used to trigger and retrieve backups." />
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="API URL" />
                            <TextBox Text="{Binding ApiUrl, Mode=TwoWay}" />
                        </StackPanel>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="RDB SERVER IP" />
                                <TextBox Text="{Binding RdbServerIp, Mode=TwoWay}" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="4">
                                <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="RDB USERNAME" />
                                <TextBox Text="{Binding RdbUsername, Mode=TwoWay}" />
                            </StackPanel>
                        </Grid>

                        <StackPanel Spacing="4">
                            <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="RDB PASSWORD" />
                            <PasswordBox x:Name="RdbPasswordBox" PasswordChanged="OnRdbPasswordChanged" />
                        </StackPanel>

                        <StackPanel Spacing="4">
                            <TextBlock Style="{StaticResource EyebrowTextStyle}" Text="BACKUP ROOT FOLDER (ON SERVER)" />
                            <TextBox Text="{Binding BackupRootFolder, Mode=TwoWay}" />
                        </StackPanel>

                        <Button Command="{Binding SaveSettingsCommand}" Content="Save Settings" HorizontalAlignment="Stretch" Style="{StaticResource GhostButtonStyle}" />

                        <StackPanel Spacing="4">
                            <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Branch selection" />
                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Search and check the branches to include in this batch job." />
                        </StackPanel>

                        <TextBox PlaceholderText="Search branch codes..." Text="{Binding BranchFilterText, Mode=TwoWay}" />

                        <ListView
                            x:Name="BranchListView"
                            Height="220"
                            IsMultiSelectCheckBoxEnabled="True"
                            ItemsSource="{Binding FilteredBranchCodes}"
                            SelectionChanged="OnBranchSelectionChanged"
                            SelectionMode="Multiple">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock VerticalAlignment="Center" Text="{Binding}" />
                                        <Button
                                            Grid.Column="1"
                                            Padding="8,4"
                                            Click="OnRemoveBranchClicked"
                                            Content="Remove"
                                            Style="{StaticResource GhostButtonStyle}"
                                            Tag="{Binding}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox PlaceholderText="New branch code" Text="{Binding NewBranchCode, Mode=TwoWay}" />
                            <Button Grid.Column="1" Command="{Binding AddBranchCodeCommand}" Content="Add" Style="{StaticResource PrimaryButtonStyle}" />
                        </Grid>

                        <Button
                            Command="{Binding TriggerJobCommand}"
                            Content="Trigger Backup Job"
                            HorizontalAlignment="Stretch"
                            Style="{StaticResource TealButtonStyle}" />
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource PanelBorderStyle}">
                <Grid RowSpacing="14">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Spacing="4">
                        <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="Batch status" />
                        <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Each branch becomes downloadable independently once its zip is detected and validated." />
                    </StackPanel>

                    <ListView Grid.Row="1" ItemsSource="{Binding Jobs}" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,4" Style="{StaticResource CardBorderStyle}">
                                    <Grid ColumnSpacing="12" RowSpacing="6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock FontWeight="Bold" Text="{Binding BranchCode}" />
                                        <Border Grid.Column="1" Style="{StaticResource StatusPillStyle}">
                                            <TextBlock FontSize="12" FontWeight="Bold" Text="{Binding Status}" />
                                        </Border>

                                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Spacing="4">
                                            <ProgressBar Maximum="1" Minimum="0" Value="{Binding DownloadProgress}" />
                                            <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="{Binding ErrorMessage}" />
                                            <Button
                                                Command="{Binding DownloadCommand}"
                                                Content="Download"
                                                HorizontalAlignment="Left"
                                                IsEnabled="{Binding CanDownload}"
                                                Style="{StaticResource PrimaryButtonStyle}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <TextBlock Grid.Row="2" Style="{StaticResource SecondaryTextStyle}" Text="{Binding StatusMessage}" />
                </Grid>
            </Border>
        </Grid>

        <Grid
            Grid.RowSpan="2"
            Background="#88000000"
            Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressRing
                Width="48"
                Height="48"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="{Binding IsBusy}" />
        </Grid>
    </Grid>
</Page>
